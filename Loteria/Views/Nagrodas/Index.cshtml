@model IEnumerable<Loteria.Models.DbModels.Nagroda>

@{
    ViewBag.Title = "Index";
}

<h2 style="color:darkslategray">Nagrody</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table" style="background-color:honeydew">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nagroda)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nagroda)
        </td>
    </tr>
}

</table>
