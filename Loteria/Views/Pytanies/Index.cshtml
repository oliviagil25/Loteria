@model IEnumerable<Loteria.Models.DbModels.Pytanie>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tresc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tresc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PytanieId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PytanieId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PytanieId })
        </td>
    </tr>
}

</table>
